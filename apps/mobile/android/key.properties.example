# Copy to key.properties and keep the real file outside version control.
storePassword=REPLACE_WITH_UPLOAD_KEYSTORE_PASSWORD
keyPassword=REPLACE_WITH_UPLOAD_KEY_PASSWORD
keyAlias=upload
storeFile=/absolute/path/to/realtime-me-upload.jks
