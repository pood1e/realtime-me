export const AUTH_API_BASE = window.location.origin;
export const STATUS_API_BASE = `${window.location.origin}/api/status`;
export const LIBRARY_API_BASE = `${window.location.origin}/api/library`;
export const MANAGER_API_BASE = `${window.location.origin}/api/manager`;
