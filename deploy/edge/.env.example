# Copy to .env on the Docker host. Keep the token itself in the referenced
# root-only file; never put the token value in this Compose environment file.

CLOUDFLARED_IMAGE=cloudflare/cloudflared:2026.7.2
CLOUDFLARE_TUNNEL_TOKEN_FILE=/etc/realtime-me/cloudflare-tunnel.token
