.gradle/
.kotlin/
build/
**/build/
local.properties
.idea/
*.iml
.DS_Store
captures/
.externalNativeBuild/
.cxx/
*.apk
*.idsig
*.keystore
*.jks
node_modules/
**/dist/
*.tsbuildinfo
.env
.env.*
!.env.example
.app-password

# Bearer token Prometheus presents to the gateway's scrape-discovery endpoint.
deploy/status/prometheus/discovery_token

# Every setting the gateway has, including all five of its tokens; and the curated
# repositories, which name private repos. Copy each .example beside them and fill it
# in — the stack bind-mounts both into the gateway.
deploy/status/gateway.yaml
deploy/status/projects.json

# Library deployment settings and runtime state.
deploy/library/runtime.env
deploy/library/cloud-drive-password.txt
*.pem
*.key
*.token
data/
backups/
staging/

# Generated per-repo project docs (may include private repo internals)
docs/projects/

# Python
__pycache__/
*.pyc

# Anthropic's Claw'd artwork. Used by the deployed page, never committed. See
# packages/status-web/src/assets/agents/NOTICE.md
packages/status-web/src/assets/agents/clawd/
packages/status-web/src/assets/agents/codex/
