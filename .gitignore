.gradle/
.kotlin/
build/
**/build/
local.properties
.idea/
*.iml
.DS_Store
captures/
.externalNativeBuild/
.cxx/
*.apk
*.idsig
*.keystore
*.jks
node_modules/
**/dist/
*.tsbuildinfo
.env
.env.*
!.env.example
.app-password

# Bearer token Prometheus presents to the gateway's scrape-discovery endpoint.
infra/status-stack/prometheus/query_token

# Every setting the gateway has, including all five of its tokens; and the curated
# repositories, which name private repos. Copy each .example beside them and fill it
# in — the stack bind-mounts both into the gateway.
infra/status-stack/gateway.yaml
infra/status-stack/projects.json

# Generated per-repo project docs (may include private repo internals)
docs/projects/

# Python
__pycache__/
*.pyc

# Anthropic's Claw'd artwork. Used by the deployed page, never committed. See
# apps/status-page/src/assets/agents/NOTICE.md
apps/status-page/src/assets/agents/clawd/
apps/status-page/src/assets/agents/codex/
